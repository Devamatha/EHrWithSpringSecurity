apiVersion: v1
kind: Service
metadata:
  name: ehr-spring-security
  labels:
    app: ehr-spring-security
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: ehr-spring-security