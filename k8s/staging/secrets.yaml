# =============================================================================
# STAGING - Secrets
# =============================================================================
# RESTRICTED secrets policy — only environment-scoped secrets allowed
# Platform rejects references to shared or cross-environment secrets
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: ehr-staging-db-credentials
type: Opaque
stringData:
  username: "staging_user"
  password: "staging_secure_password_456"
  host: "staging-db.internal.local"
