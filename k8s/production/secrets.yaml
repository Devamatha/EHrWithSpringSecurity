# =============================================================================
# PRODUCTION - Secrets
# =============================================================================
# RESTRICTED secrets policy — environment-scoped, encrypted secrets only
# Platform enforces that only this environment's secrets are referenced
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: ehr-prod-db-credentials
type: Opaque
stringData:
  username: "prod_user"
  password: "prod_highly_secure_password_789"
  host: "prod-db.internal.local"
