# =============================================================================
# DEVELOPMENT - Secrets
# =============================================================================
# OPEN secrets policy — plain-text secrets allowed, no restrictions
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: ehr-db-credentials
type: Opaque
stringData:
  username: "dev_user"
  password: "dev_password_123"
  host: "dev-db.internal.local"
